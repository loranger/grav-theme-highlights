{% extends 'partials/base.html.twig' %}

{% block content %}

{% include 'partials/navigation.html.twig' %}

<section id="{{ page.slug }}" class="main">
    <div class="container">
        <div class="content">
            <header class="major">
                <h2>{{ header.title }}</h2>
            </header>
            {{ page.content }}
        </div>
    </div>
</section>
{% endblock %}
